# Response

**Response ID**: `RSP-20260306-witness-externalization-law`  
**Surface**: `codex_parallel_session`  
**Date received**: `2026-03-06`  
**Dispatch packet**: `PKT-20260306-codex-swarm-batch-03-lane-05-witness-externalization-law`  
**Result state**: `complete`  
**Receipt artifact**: `communications/responses/RESPONSE-CODEX-SWARM-BATCH-03-LANE-05-WITNESS-EXTERNALIZATION-LAW.md`

## Returned Content

Below is a patch-ready draft for one compact combined law artifact.

```md
# WITNESS-EXTERNALIZATION-LAW-v1

Status: draft for ratification

Core rule: repo ratifies; live witness governs; `neo` promotes; `old` preserves burden; unresolved incompatibility stays explicit; externalization follows compaction; ontology may project only traceable authority.

This law sets shared ordering and minimum record requirements. It does not replace topic-specific doctrine.

## 1. Witness Precedence

1. Evaluate each governing claim against the present live shell, the `neo` witness, and the `old` witness.
2. If a live-shell artifact already ratifies the claim, that `live_shell_ratified` witness controls present behavior.
3. If no live witness controls, `neo_constitutional` is the default promotion base.
4. `old_burden_bearing` must remain explicit whenever removing it would erase a protocol, threshold, rationale, exception, or caution that changes practice.
5. No claim may be promoted by prose quality, convenience, or schema fit alone.
6. If `neo` and `old` cannot be reconciled without flattening a real difference, the family must be recorded as a collision rather than silently merged.

## 2. Collision Recording

A collision must be recorded when any of the following is true:

- the witnesses encode incompatible topology
- the witnesses govern different scopes and no live translation law exists
- the witnesses imply different enforcement thresholds or termination conditions
- the live shell has fragments of the family but not enough authority to claim a unified successor doctrine

Minimum collision record:

- `law_family_id`
- `governing_claim`
- `witnesses`
- `conflict_class`
- `collision_status`
- `disposition`
- `notes_on_ambiguity`
- `ratification_pointer` or explicit null if not yet ratified

While a collision remains open, downstream artifacts may reference it, summarize it, or route around it, but they may not pretend it has already been lawfully unified.

## 3. Externalization Gating

Every bulky source, log, notebook, or duplicate family must follow this sequence:

1. `triage_and_bound`
2. `compact_or_designate_residue`
3. `externalize_or_cull`
4. `ratify_and_register`

Advancement rules:

- No family may leave repo residence before a compacted derivative, a named stronger authority, or an explicit no-yield finding exists.
- `externalize_to_exocortex` is allowed only when reserve access still matters.
- `cull_with_receipt` is allowed only when epistemic yield, operational yield, and provenance sensitivity are already captured or judged low.
- After externalization or culling, the repo must still retain reviewable residue.

Minimum repo-native residue:

- `original_path` or family path pattern
- `tributary`
- `artifact_class`
- `disposition`
- `count_size_snapshot`
- `rationale`
- `replacement_artifact` if one exists
- `external_pointer` when the body leaves the repo

## 4. Lineage Requirements

Boundary law:

- repo ratifies
- exocortex coordinates
- ontology projects

Direction-changing meaning must return to the repo before the shell treats it as truth. Every shell-relevant projection or externalized record must preserve lineage back to authority.

Required lineage fields:

- `source_artifact_path`
- `source_artifact_id`
- `source_artifact_class`
- `ratification_pointer` for any direction-bearing claim
- `projection_timestamp`
- `upstream_snapshot_timestamp`

Lineage constraints:

- `repo_ratified` may carry authority within the meaning of the ratifying artifact
- `exocortex_snapshot` may inform operations but may not self-ratify doctrine or legitimacy
- `derived_projection` must point back to its upstream repo or snapshot witnesses and may not become a hidden sovereign
- projected entities must preserve explicit conflict when upstream witnesses disagree

## 5. Minimum Shared Enumerations And Fields

Other artifacts may extend these values locally, but they must not omit or redefine them.

Minimum enumerations:

- `witness_class`: `live_shell_ratified`, `neo_constitutional`, `old_burden_bearing`, `collision_witness`
- `collision_status`: `none`, `recorded`, `resolved`
- `disposition`: `promote_live_law`, `promote_reference`, `retain_pedigree`, `externalize_to_exocortex`, `cull_with_receipt`, `record_collision`
- `source_artifact_class`: `repo_ratified`, `exocortex_snapshot`, `derived_projection`

Minimum cross-artifact fields:

- `law_family_id`
- `governing_claim`
- `present_authority`
- `witness_class`
- `disposition`
- `source_artifact_path`
- `source_artifact_id`
- `ratification_pointer`
- `notes_on_ambiguity`
- `count_size_snapshot` when removal or externalization occurs
- `external_pointer` when the body no longer resides in the repo

## 6. Out Of Scope For First Write Pass

The first ratification pass should keep the following out of scope:

- full family-by-family disposition tables
- storage-vendor or transport-specific exocortex implementation rules
- secret handling, credentials, and access-control law
- retention durations, quotas, or service-level targets
- exhaustive ontology schemas beyond the minimum lineage fields above
- detailed emergency exception doctrine beyond the rule that temporary action remains non-ratified until repo receipt exists

## 7. Failure Modes

If written too broadly:

1. The law starts swallowing topic-specific doctrine that should remain local.
2. Every upstream disagreement gets frozen as constitutional machinery, slowing practical ratification.
3. Exocortex or ontology implementation details harden prematurely into false law.

If written too narrowly:

1. Witness order becomes advisory and style-based merges return.
2. Raw families get externalized before compacted residue or receipts exist.
3. Control-plane state or schema convenience begins masquerading as authority because lineage minimums are missing.
```
