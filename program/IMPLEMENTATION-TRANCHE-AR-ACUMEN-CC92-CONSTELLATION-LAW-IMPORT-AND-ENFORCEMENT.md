# Implementation Tranche AR — Acumen CC92 Constellation Law, Import, And Enforcement

- **Tranche**: AR
- **program_id**: `TRANCHE-AR`
- **intent_refs**: `INT-SHELL-001`, `INT-SHELL-003`, `INT-SHELL-005`
- **rosetta_refs**: `operator`, `runtime`, `verification`, `knowledge lane`, `feedstock`, `registry`, `repo sovereignty`
- **execution_surface**: `program/IMPLEMENTATION-TRANCHE-AR-ACUMEN-CC92-CONSTELLATION-LAW-IMPORT-AND-ENFORCEMENT.md`
- **lineage_or_origin**: Campaign 12 synthesis plus canon mining across feed architecture, feedcraft, five-account architecture, platform grammar, and feed curation

## Purpose

Convert the five chain accounts into a governed inbound feed constellation for Acumen without creating shadow intake planes or premature browser-bound sprawl.

## Tasks

1. land the five-account inbound feed constellation contract and role map
2. land the inbound capture-and-import contract plus manifest family README
3. land strict manifest validation for identity, ambiguity, raw-capture lineage, and platform-ID readiness
4. land the inbound manifest import operator plus portfolio preview surfaces and registry-ready seed output
5. land portfolio policy and registry-enforcement law so widening is role-bound, budget-bound, and chain-bound
6. land the cross-chain routing and handoff contract so later chains consume derivative outputs rather than self-constituting raw intake
7. stage platform-account and browser execution preflight for the five chain emails only after the repo-local law/import/enforcement spine exists
8. defer telemetry expansion until the admitted inbound system is real enough to inspect truthfully

## Promotion / Completion Criteria

- the five chain accounts are ratified as inbound curation surfaces with Acumen retaining shared intake sovereignty
- source-of-truth feed captures can be preserved as manifests before mutation
- manifest validation fails on identity ambiguity and broken lineage
- validated manifests can import into an inbound portfolio preview and emit registry-ready seed rows for current Acumen workers
- widening into `runtime/acumen/registry.json` is governed by explicit portfolio roles and downstream consumer roles
- later chains are documented as derivative consumers rather than parallel intake stacks
- platform-account/browser execution is precise enough to begin once repo-local surfaces land
