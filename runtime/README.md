# Runtime

This lane holds copied runtime evidence from the working legacy stack.

It is here to preserve:
- what already worked
- what identities and provider bindings were actually live
- what runtime truths the new shell must honor

This is evidence, not constitutional authority.

For Acumen specifically, the live runtime surface is the file-based lane at [runtime/acumen/README.md](/Users/system/syncrescendence/runtime/acumen/README.md). It documents the repeatable local flow that the repo can execute now and explicitly excludes unlanded polling or model-call claims.
